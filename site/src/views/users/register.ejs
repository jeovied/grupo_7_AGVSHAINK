<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head') %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
</head>

<body class="register">
    <header>
        <%- include('../partials/header') %>
    </header>
    <h2 style="margin: 20px auto;">Ingreso de usuario</h2>
    <main>
        <h3>Crea tu cuenta</h3>
        <div>
            <h6>Datos de usuario</h6>
            <p>Si ya estas registrado <a href="/users/login">click aca</a></p>
        </div>
        <form method="POST" enctype="multipart/form-data">
            <div class="mb-3">
                <label for="exampleInputName1" class="form-label">Nombre</label>
                <input type="text" name="name" placeholder="Jose" value="<%= locals.old && old.name ? old.name : null %>" class="form-control" id="exampleInputName1" aria-describedby="emailHelp">
                <% if (locals.errors) { %>
                 <% errors.forEach(error => { %>
                    <span><%- error.param === "name" ? error.msg : null %> </span>
                 <% }) %>
                <% } %>
              </div>
            <div class="mb-3">
                <label for="exampleInputLastName1" class="form-label">Apellido</label>
                <input type="text" name="last_name" placeholder="Perez" value="<%= locals.old && old.lastname ? old.lastname : null %>" class="form-control" id="exampleInputLastName1" aria-describedby="emailHelp">
                <% if (locals.errors) { %>
                    <% errors.forEach(error => { %>
                       <span><%- error.param === "lastname" ? error.msg : null %> </span>
                    <% }) %>
                   <% } %>
              </div>
            <div class="mb-3">
              <label for="exampleInputEmail1" class="form-label">Email</label>
              <input type="email" name="email" placeholder="joseperez@gmail.com" value="<%= locals.old && old.email ? old.email : null %>" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp">
              <% if (locals.errors) { %>
                <% errors.forEach(error => { %>
                   <span><%- error.param === "email" ? error.msg : null %> </span>
                <% }) %>
               <% } %>
               <% if (locals.errorEmail) { %>
                <span><%= errorEmail.msg %></span>
            <% } %>
            </div>
            <div class="mb-3">
              <label for="exampleInputPassword1" class="form-label">Contraseña</label>
              <input type="password" name="password" class="form-control" id="exampleInputPassword1">
              <% if (locals.errors) { %>
                <% errors.forEach(error => { %>
                   <span><%- error.param === "password" ? error.msg : null %> </span>
                <% }) %>
               <% } %>
            </div>
            <div class="mb-3">
                <label for="exampleInputPhoneNumber1" class="form-label">Número de telefono</label>
                <input type="number" name="number" placeholder="15-12345678" value="<%= locals.old && old.number ? old.number : null %>" class="form-control" id="exampleInputPhoneNumber1" aria-describedby="emailHelp">
                <% if (locals.errors) { %>
                    <% errors.forEach(error => { %>
                       <span><%- error.param === "number" ? error.msg : null %> </span>
                    <% }) %>
                   <% } %>
              </div>
              <div class="mb-3">
                <label for="formFile" class="form-label">Avatar</label>
                <input class="form-control" type="file" name="image" id="formFile">
              </div>
            <div style="justify-content: center; flex-direction: column; align-items: center;"><button type="submit" class="btn btn-primary" style="margin: 0px auto; display: flex; justify-content: center; flex-direction: column; align-items: center;">Registrate</button></div>
          </form>
            
    </main>
    <div>
        <article class="insta">
            <i class="fab fa-instagram"></i>
            <h3>instagram</h3>
            <section>
                <div>
                    <img src="/images/pokemon.jpeg" alt="">
                    <img src="/images/pokemon.jpeg" alt="">
                </div>
                <div>
                    <img src="/images/pokemon.jpeg" alt="">
                    <img src="/images/pokemon.jpeg" alt="">
                </div>
            </section>
        </article>
    </div>
    <footer>
        <%- include('../partials/footer') %>
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
</body>

</html>